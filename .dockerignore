# ==============================================================================
# .dockerignore - PENJELASAN LENGKAP
# ==============================================================================
# File ini seperti .gitignore tapi untuk Docker.
# Fungsi: Memberitahu Docker file/folder mana yang TIDAK perlu di-copy ke image.
# Manfaat:
# 1. Build lebih cepat (file yang di-copy lebih sedikit)
# 2. Image size lebih kecil
# 3. Security lebih baik (tidak expose file sensitive)
# ==============================================================================

# ------------------------------------------------------------------------------
# NODE.JS & NPM
# ------------------------------------------------------------------------------
# node_modules tidak perlu di-copy karena kita install ulang di Dockerfile
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# NOTE: package-lock.json PERLU di-copy untuk npm ci (jangan exclude!)

# ------------------------------------------------------------------------------
# NEXT.JS BUILD OUTPUT
# ------------------------------------------------------------------------------
# Build output lama tidak perlu (kita build ulang di Dockerfile)
.next
out
dist
build

# ------------------------------------------------------------------------------
# ENVIRONMENT FILES
# ------------------------------------------------------------------------------
# File .env berisi rahasia (API keys, passwords, dll)
# JANGAN PERNAH masukkan ke Docker image!
.env
.env.local
.env.*.local
.env.development
.env.production

# ------------------------------------------------------------------------------
# GIT
# ------------------------------------------------------------------------------
# File Git tidak perlu di production
.git
.gitignore
.gitattributes

# ------------------------------------------------------------------------------
# DOCKER FILES
# ------------------------------------------------------------------------------
# Dockerfile sendiri tidak perlu ada di dalam image
Dockerfile
docker-compose.yml
.dockerignore

# ------------------------------------------------------------------------------
# IDE & EDITOR
# ------------------------------------------------------------------------------
# File config editor tidak perlu
.vscode
.idea
*.swp
*.swo
*.swn
.DS_Store

# ------------------------------------------------------------------------------
# TESTING
# ------------------------------------------------------------------------------
# File test tidak perlu di production
**/*.test.js
**/*.test.ts
**/*.test.tsx
**/*.spec.js
**/*.spec.ts
**/*.spec.tsx
coverage
.nyc_output

# ------------------------------------------------------------------------------
# LOGS
# ------------------------------------------------------------------------------
# Log files tidak perlu
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# ------------------------------------------------------------------------------
# DOCUMENTATION
# ------------------------------------------------------------------------------
# File markdown dokumentasi tidak perlu di production (opsional)
# Uncomment jika ingin exclude semua MD files
# *.md
README.md
CHANGELOG.md

# ------------------------------------------------------------------------------
# TEMPORARY FILES
# ------------------------------------------------------------------------------
# File temporary & cache
*.tmp
*.temp
.cache
.parcel-cache
.eslintcache

# ------------------------------------------------------------------------------
# WINDOWS SPECIFIC
# ------------------------------------------------------------------------------
# File Windows yang tidak perlu
Thumbs.db
desktop.ini
*.bat

# ------------------------------------------------------------------------------
# DATABASE
# ------------------------------------------------------------------------------
# Database local development
*.sqlite
*.sqlite3
*.db

# ------------------------------------------------------------------------------
# MISC
# ------------------------------------------------------------------------------
# File-file lain yang tidak perlu
.editorconfig
.prettierrc
.eslintrc
.babelrc
tsconfig.tsbuildinfo

# ==============================================================================
# CATATAN PENTING
# ==============================================================================
#
# 1. SELALU exclude node_modules karena kita install ulang di Dockerfile
# 2. WAJIB exclude .env files untuk keamanan
# 3. Semakin sedikit file yang di-copy, semakin cepat build time
# 4. File yang diabaikan di sini TIDAK ADA di dalam Docker image
#
# Untuk cek apa saja yang akan di-copy:
# docker build --no-cache --progress=plain -t test .
#
# ==============================================================================
